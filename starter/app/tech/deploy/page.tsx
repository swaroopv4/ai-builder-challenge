export default function TechDeployPage() {
  return (
    <div className="p-2">
      <h1 className="text-2xl font-bold">Deploy (stub)</h1>
      <p className="text-gray-600 mt-2">
        Build the deploy workflow here. Scan an asset tag, scan a rack +
        rack-unit destination, transition the asset to <code>in_service</code>.
        Deploy requires a complete location (rack + ru). See{" "}
        <code>docs/tips.md</code>.
      </p>
    </div>
  );
}
